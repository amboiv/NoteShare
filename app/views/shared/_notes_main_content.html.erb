<h3 class=" col-md-12 border-bottom">
  <%= show_main_title %> 
</h3>

<div class="col-md-8">
    <%= render 'shared/notes_cards' if @course %>

   
  <%= paginate @notes %> 

  <%= render @notes %> 

  <%= paginate @notes %> 

</div>


 


<div class="col-md-4">
    <%= render 'shared/notes_sidebar' if @course %>
</div>
